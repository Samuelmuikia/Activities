// Named Parameters
// write a function that takes two named parameters:
// print each named parameter,
// then return the parameters added together
function myParameters
function myParameters ("This is param1: " + pram1, "This is param2: " +  pram2) {
console.log(pram1, pram2);
return pram1 + pram2;
}
// invoke the function and pass in two numbers
myFunction(1,2);
// invoke the function and pass in more than two numbers
myFunction(1,2,3,4);
// invoke the function and pass in only one number
myFunction(1)
// change the function to set default values for the parameters
function myParameters ("This is param1: " + pram1, "This is param2: " +  pram2)
// again, invoke the function and pass in only one number

// Rest Operator
console.log(pram1, pram2, ...Param)
// add a rest operator to the function's parameters
function  myParameters (pram1, pram2, ...Param1, ...Param2)
// add a line to the function's body to print the value of the rest operator
function myParameters
function myParameters ("This is param1: " + pram1, "This is param2: " +  pram2  ...Param1, ...Param2) {
console.log(pram1, pram2  ...Param1, ...Param2);
return pram1 + pram2;
// again, invoke the function and pass in more than two numbers
myFunction(1,2,3,4);
